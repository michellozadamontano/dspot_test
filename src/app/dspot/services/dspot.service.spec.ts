import { HttpClient } from "@angular/common/http";
import { DspotService } from "./dspot.service";

describe('DspotService', () => {
    let service: DspotService;
    let http   : HttpClient;
    beforeEach(() => { service = new DspotService(http); });
  
  
  
    
  });